services:
  valkey:
    image: valkey/valkey:8-alpine
    container_name: homelab07-valkey
    restart: unless-stopped
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    command:
      - valkey-server
      - --save
      - ""
      - --appendonly
      - "no"
      - --protected-mode
      - "no"
    tmpfs:
      - /data:rw,noexec,nosuid,nodev,size=64m
    networks:
      - homelab07-internal
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 10s

networks:
  homelab07-internal:
    name: homelab07-internal
    external: true
