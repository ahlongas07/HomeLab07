services:
  landing-page:
    image: nginx:1.27-alpine
    container_name: homelab07-landing-page
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
